name: Deploy to IONOS Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Deploy to IONOS server
      uses: /easingthemes/ssh-deploy@main
      with:
        REMOTE_HOST: access951720039.webspace-data.io
        REMOTE_USER: u111236921
        SSH_PRIVATE_KEY: suckmyballstrump
        TARGET: ./
        SOURCE: ./